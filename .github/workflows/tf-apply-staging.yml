name: Terraform Apply — Staging

on:
  push:
    branches: [staging]

env:
  TF_VERSION: "1.7.0"
  GOOGLE_CREDENTIALS: ${{ secrets.GCP_SA_KEY_STAGING }}

jobs:
  apply-staging:
    name: Apply to Staging
    runs-on: ubuntu-latest
    environment: staging
    defaults:
      run:
        working-directory: environments/staging
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
      - name: Terraform Init
        run: terraform init
      - name: Terraform Apply
        run: |
          terraform apply -auto-approve \
            -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
            -var="region=${{ secrets.GCP_REGION }}" \
            -var="vpc_network=${{ secrets.VPC_NETWORK }}" \
            -var="api_image=${{ secrets.API_IMAGE_STAGING }}" \
            -var="service_account_email=${{ secrets.SA_EMAIL }}" \
            -var="db_password=${{ secrets.DB_PASSWORD_STAGING }}"
